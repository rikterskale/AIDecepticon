services:
  aidecepticon:
    build: .
    ports:
      - "8787:8787"
    environment:
      PORT: 8787
      DATA_DIR: /data
      PUBLIC_BASE_URL: http://localhost:8787
    volumes:
      - aidecepticon-data:/data
    restart: unless-stopped

volumes:
  aidecepticon-data:
